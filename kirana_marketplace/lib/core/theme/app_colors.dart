import 'package:flutter/material.dart';

/// Central colour palette. A warm green/orange pairing reads as
/// "neighbourhood grocery" rather than a generic food-delivery brand.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF2E7D32); // kirana green
  static const Color primaryDark = Color(0xFF1B5E20);
  static const Color accent = Color(0xFFEF6C00); // warm orange
  static const Color background = Color(0xFFF7F7F2);
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF1B1B1B);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFEF6C00);
  static const Color danger = Color(0xFFC62828);
  static const Color pending = Color(0xFF8D6E63);
  static const Color divider = Color(0xFFE0E0E0);
}
